include_directories(${CMAKE_CURRENT_SOURCE_DIR}/../include)
link_directories(${CMAKE_CURRENT_BINARY_DIR}/../lib)

add_executable(
   ppm_cmp
   ppm.c
   ppm_cmp.c
)

add_executable(
   tiles
   tiles.c
   ppm.c
)

add_executable(
   udta_gen
   udta_gen.c
)

target_link_libraries(tiles ${LIBPUD_LIBRARIES})
target_link_libraries(udta_gen ${LIBPUD_LIBRARIES})

